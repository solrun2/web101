// var ค่าเปลี่ยนแปลงได้ scope เป็น global ไม่ควรใช้
// let ค่าเปลี่ยนแปลงได้ scope เป็น local ควรใช้
// const ค่าคงที่ ไม่สามารถเปลี่ยนแปลงได้ ควรใช้ local

/* let customerName: string; // ตัวแปรปกติ camelCase 
customerName = "John"; // กำหนดค่าให้ตัวแปร

const MATHPI = 3.14; // ตัวแปรคงที่ ใช้ตัวใหญ่ทั้งหมด snake_case

console.log(customerName); */

/* let x:number = 5;
let y:number = 10;

if (x > 5) {
    console.log("x > 5");
} else {
    console.log("x <= 5");
}

console.log(x>5 ? "x > 5" : "x <= 5"); */

/* let text1: string = "A";
let text2: string = "B";

let firstname:string = "John";
let lastname:string = "Doe";

let age:number = 10;

// console.log("Hello",firstname,lastname,"age",age);
console.log(`Hello ${firstname} ${lastname} age ${age}`); */

/* let x: number = 5;

if (x===5) {
    console.log(true);
} else {
    console.log(false);
}   

switch (x) {
    case 5:
        console.log("x === 5");
        break;
    
    default:
        break;
}

while (x > 2) {
    console.log(x);
    x--;
}

for(let i = 0; i < 10; i++) {
    console.log(i);
} */